import SectionTitle from ".";

export default {
    title: "atoms/SectionTitle",
    component: SectionTitle,
}

const title = "Section title"

export const Dark = () =>  <SectionTitle title={title}/>